# Employee Organizational Analysis – Java SE + Maven

This project analyzes an organization's structure based on a CSV employee file.

## How to Run
mvn clean install
mvn exec:java -Dexec.mainClass="com.company.App"
